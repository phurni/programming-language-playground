fun do_it(first_arg, second_one) {
  other(100 + 100)
  any_function(some_var, other(99) <= second_one)
}

fun other() {
  while (1) {
  }
  if (some <= calling(99)) {
    any_function()
  }
  else {
    do_it()
  }
}
